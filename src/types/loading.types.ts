export type LTypes = "blank" | "balls" | "bars" | "bubbles" | "cubes" | "cylon" | "spin" | "spinningBubbles" | "spokes";
export type TLoadingProps = {
     type : LTypes,
     color : string,
     scale : number
}